# Copy this file to keystore.properties and fill the real local values.
# Never commit keystore.properties or the keystore file.

storeFile=release/retrosprite-release.jks
storePassword=REPLACE_WITH_A_STRONG_LOCAL_PASSWORD
keyAlias=retrosprite-release
keyPassword=REPLACE_WITH_A_STRONG_LOCAL_PASSWORD
