#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ "${1:-}" == "coverage" ]]; then
  shift
  exec python3 "$ROOT_DIR/scripts/gkp_lite_coverage.py" "$@"
fi

exec python3 "$ROOT_DIR/scripts/gkp_builder_new.py" "$@"
